{
  "name": "codex-openrouter-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local OpenRouter relay with an OpenAI-compatible chat completions endpoint.",
  "scripts": {
    "dev:web": "vite --host 127.0.0.1",
    "build": "vite build",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@ant-design/icons": "^6.0.0",
    "antd": "^6.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
